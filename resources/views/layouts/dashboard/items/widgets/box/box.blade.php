<div id="{{ $id }}" class="box {{ $class }}">
    <h4 class="box-header {{ $hclass }}"><i class="{{$icon}}"></i> {{ $title }}</h4>
    <div class="box-body {{ $bclass }}">{!! $description !!}</div>
</div>
